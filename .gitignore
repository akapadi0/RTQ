node_modules
*.log

# dist/ IS committed — PlannerXchange requires the built distRoot output
# (including plannerxchange.publish.json and plannerxchange.build-provenance.json)
# in the repo so it can bind hosted artifacts to the reviewed source commit.
